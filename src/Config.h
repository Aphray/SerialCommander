#ifndef RX_BUFFER_SIZE
    #define RX_BUFFER_SIZE 64
#endif

#ifndef TX_BUFFER_SIZE
    #define TX_BUFFER_SIZE 64
#endif

#ifndef MAX_COMMANDS
    #define MAX_COMMANDS 5
#endif

#ifndef MAX_CALLBACKS_PER_CMD
    #define MAX_CALLBACKS_PER_CMD 3
#endif

#ifndef COMMAND_NAME_MAX_LEN
    #define COMMAND_NAME_MAX_LEN 5
#endif